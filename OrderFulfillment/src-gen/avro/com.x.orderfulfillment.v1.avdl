/**
 * This is a autogenerated using the com.x.ocl:xocl-maven-plugin:1.4.2 maven plugin.
 * The contents are generated from the following XOCL files:
 *            orderfulfillment.messages.xocl
 *            orderfulfillment.transactions.xocl
 *            orderfulfillment.processes.xocl
 *            orderfulfillment.contracts.xocl
 * DO NOT EDIT THIS FILE DIRECTLY
 */
@namespace("com.x.orderfulfillment.v1")
protocol com_x_orderfulfillment_v1 {
	import idl "com.x.core.v1.avdl";

	@security("confidential")
	record CustomerInfo {
		union{string, com.x.core.v1.EntityId} id;
 		com.x.core.v1.Name name;
 		union{null,com.x.core.v1.Email} email = null;
 		union{null,com.x.core.v1.Phone} phone = null;
  	}

	@topics("/com.x.orderfulfillment.v1/OrderFulfillment/MerchantComment")
	@version("1.0.0")
	record MerchantComment {
		//A GUID that is 32-char hexidecimal string
		string orderId;
 		union{null,string} orderLineId = null;
 		string comment;
  	}

	enum FulfillmentOrderStatus {
		NEW,
		READY_TO_SHIP,
		PARTIALLY_SHIPPED,
		SHIPPED,
		PARTIALLY_CANCELLED,
		CANCELLED,
		COMPLETED
	}

	record GiftTag {
		union{null,string} from = null;
 		union{null,string} to = null;
 		string giftMessage;
  	}

	@security("confidential")
	record ShipTo {
		com.x.core.v1.Name name;
 		com.x.core.v1.Address address;
 		GiftTag giftTag;
  	}

	record FulfillmentOrderLineData {
		string orderLineId;
 		union{null,string} itemId = null;
 		union{null,string} orderNumber = null;
 		float quantity;
 		string productSku;
 		string itemDescription;
 		union{null,com.x.core.v1.DateRange} expectedShipDate = null;
 		union{null,com.x.core.v1.CurrencyAmount} unitPrice = null;
 		FulfillmentOrderStatus status;
 		union{null,com.x.core.v1.CurrencyAmount} price = null;
 		//A string representing the date and time as defined by RFC 3339's 'date-time' format with 'time-secfrac' defined.
		union{null,string} dateCancelled = null;
 		//A string representing the date and time as defined by RFC 3339's 'date-time' format with 'time-secfrac' defined.
		union{null,string} dateDelivered = null;
 		//A string representing the date and time as defined by RFC 3339's 'date-time' format with 'time-secfrac' defined.
		union{null,string} dateShipped = null;
 		ShipTo destination;
  	}

	@topics("/com.x.orderfulfillment.v1/OrderFulfillment/ItemCancelled")
	@version("1.0.0")
	record ItemCancelled {
		//A GUID that is 32-char hexidecimal string
		string orderId;
 		FulfillmentOrderLineData orderLine;
 		union{null,string} reasonCode = null;
 		union{null,string} cancelMessage = null;
  	}

	record FulfillmentOrderData {
		string orderNumber;
 		array<FulfillmentOrderLineData> orderLines;
 		FulfillmentOrderStatus status;
 		string dateOrdered;
 		union{null,com.x.core.v1.CurrencyAmount} grandTotal = null;
 		string sourceId;
 		string sourceGroupId;
 		union{null,com.x.core.v1.DateRange} expectedShipDate = null;
 		CustomerInfo customer;
 		com.x.core.v1.Address billingAddress;
 		ShipTo destination;
  	}

	record FulfillmentOrder {
		//A GUID that is 32-char hexidecimal string
		string orderId;
 		FulfillmentOrderData order;
  	}

	@topics("/com.x.orderfulfillment.v1/OrderFulfillment/OrderFulfilled")
	@version("1.0.0")
	record OrderFulfilled {
		FulfillmentOrder order;
  	}

	@topics("/com.x.orderfulfillment.v1/OrderFulfillment/FulfillOrder")
	@version("1.0.0")
	record FulfillOrder {
		FulfillmentOrder order;
  	}

	@topics("/com.x.orderfulfillment.v1/OrderFulfillment/DropShipSupplierComment")
	@version("1.0.0")
	record DropShipSupplierComment {
		//A GUID that is 32-char hexidecimal string
		string orderId;
 		union{null,string} orderLineId = null;
 		string comment;
  	}

	record ShippingDetail {
		string shippingMethod;
 		union{null,string} shippingMessage = null;
  	}

	@topics("/com.x.orderfulfillment.v1/OrderFulfillment/ItemExpectedShipDate")
	@version("1.0.0")
	record ItemExpectedShipDate {
		//A GUID that is 32-char hexidecimal string
		string orderId;
 		string orderLineId;
 		com.x.core.v1.DateRange expectedShipDate;
  	}

	record TrackingDetail {
		array<string> trackingNumbers;
 		string carrier;
 		string service;
  	}

	@topics("/com.x.orderfulfillment.v1/OrderFulfillment/ItemShipped")
	@version("1.0.0")
	record ItemShipped {
		string shipmentId;
 		//A GUID that is 32-char hexidecimal string
		string orderId;
 		string orderLineId;
 		int quantity;
 		//A string representing the date and time as defined by RFC 3339's 'date-time' format with 'time-secfrac' defined.
		string dateShipped;
 		ShippingDetail shippingDetails;
 		union{null,TrackingDetail} trackingDetails = null;
  	}


}